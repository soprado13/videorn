import {Dimensions} from 'react-native'

const win = Dimensions.get('window');
export const w = win.width;
export const h = win.height;

export const url = 'https://raw.githubusercontent.com/soprado13/videorn/master/video.json';